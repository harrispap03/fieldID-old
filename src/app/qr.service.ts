import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class QrService {

  constructor() { }

  addNewUserToList(userId){
    // Here we'll add the new user to the list
  }
}
